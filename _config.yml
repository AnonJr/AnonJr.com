# Theme Settings
#
# Review documentation to determine if you should use `theme` or `remote_theme`
# https://mmistakes.github.io/minimal-mistakes/docs/quick-start-guide/#installing-the-theme

# theme                  : "minimal-mistakes-jekyll"
# remote_theme           : "mmistakes/minimal-mistakes"
minimal_mistakes_skin    : "default" # "air", "aqua", "contrast", "dark", "dirt", "neon", "mint", "plum", "sunrise", "catppuccin_latte", "catppuccin_mocha"

# Site Settings
locale                   : "en-US"
direction                : # "ltr" (default), "rtl" # set the direction of the page
#title                   : Defined in _config.dev.yml and _config.prod.yml
title_separator          : "|"
subtitle                 : "[Insert witty by-line here]" # site tagline that appears below site title in masthead
name                     : "Mark Bussell Jr. (AnonJr)"
description              : "The personal website of AnonJr"
#url                     : Defined in _config.dev.yml and _config.prod.yml; the base hostname & protocol for your site e.g. "https://mmistakes.github.io"
baseurl                  : # the subpath of your site, e.g. "/blog"
repository               : "AnonJr/AnonJr.com" # GitHub username/repo-name e.g. "mmistakes/minimal-mistakes"
teaser                   : # path of fallback teaser image, e.g. "/assets/res/500x300.png"
logo                     : #"/assets/res/favicon.svg" # path of logo image to display in the masthead, e.g. "/assets/res/88x88.png"
masthead_title           : "AnonJr.com" # overrides the website title displayed in the masthead, use " " for no title
breadcrumbs              : false # true, false (default)
words_per_minute         : 200
enable_copy_code_button  : true # true, false (default)
copyright                : # "copyright" name, defaults to site.title
copyright_url            : # "copyright" URL, defaults to site.url

comments:
  provider               : # false (default), "disqus", "discourse", "facebook", "staticman", "staticman_v2", "utterances", "giscus", "custom"
  disqus:
    shortname            : # https://help.disqus.com/customer/portal/articles/466208-what-s-a-shortname-
  discourse:
    server               : # https://meta.discourse.org/t/embedding-discourse-comments-via-javascript/31963 , e.g.: meta.discourse.org
  facebook:
    # https://developers.facebook.com/docs/plugins/comments
    appid                :
    num_posts            : # 5 (default)
    colorscheme          : # "light" (default), "dark"
  utterances:
    theme                : # "github-light" (default), "github-dark"
    issue_term           : # "pathname" (default)
  giscus:
    repo_id              : # Shown during giscus setup at https://giscus.app
    category_name        : # Full text name of the category
    category_id          : # Shown during giscus setup at https://giscus.app
    discussion_term      : # "pathname" (default), "url", "title", "og:title"
    reactions_enabled    : # '1' for enabled (default), '0' for disabled
    theme                : # "light" (default), "dark", "dark_dimmed", "transparent_dark", "preferred_color_scheme"
    strict               : # 1 for enabled, 0 for disabled (default)
    input_position       : # "top", "bottom" # The comment input box will be placed above or below the comments
    emit_metadata        : # 1 for enabled, 0 for disabled (default) # https://github.com/giscus/giscus/blob/main/ADVANCED-USAGE.md#imetadatamessage
    lang                 : # "en" (default)
    lazy                 : # true, false # Loading of the comments will be deferred until the user scrolls near the comments container.
  staticman:
    branch               : # "master"
    endpoint             : # "https://{your Staticman v3 API}/v3/entry/github/"

reCaptcha:
  siteKey                :
  secret                 :

# Jekyll Feed: https://github.com/jekyll/jekyll-feed
feed:
  path                   : "/rss.xml" # blank (default) uses feed.xml
  posts_limit            : 15
  hide                   : # true, false (default) - Can't remember if I changed the reference to this in the include.
# Minimal Mistakes has some similar settings, but only used by the theme logic - not the plugin.
atom_feed:
  path                   : "/rss.xml" # blank (default) uses feed.xml
  hide                   : # true, false (default)

search                   : # true, false (default)
search_full_content      : # true, false (default)
search_provider          : # lunr (default), algolia, google
lunr:
  search_within_pages    : # true, false (default)
algolia:
  application_id         : # YOUR_APPLICATION_ID
  index_name             : # YOUR_INDEX_NAME
  search_only_api_key    : # YOUR_SEARCH_ONLY_API_KEY
  powered_by             : # true (default), false
google:
  search_engine_id       : # YOUR_SEARCH_ENGINE_ID
  instant_search         : # false (default), true

# SEO Related
google_site_verification :
bing_site_verification   :
naver_site_verification  :
yandex_site_verification :
baidu_site_verification  :

# Social Sharing
twitter:
  username               : "AnonJr"
  card                   : # The site's default card type

facebook:
  username               :
  app_id                 :
  publisher              :

og_image                 : "/assets/res/Profile_800x800.png" # Open Graph/Twitter default site image
og_image_alt             : # Alt text for Open Graph/Twitter default site image

# For specifying social profiles - https://developers.google.com/structured-data/customize/social-profiles
social:
  type                   : Person # Person or Organization (defaults to Person)
  name                   : # If the user or organization name differs from the site's name
  links: # An array of links to social media profiles

# Analytics
analytics:
  provider               : # false (default), "google", "google-universal", "google-gtag", "custom"
  google:
    tracking_id          :
    anonymize_ip         : # true, false (default)


# Site Author
author:
  name                   : "Mark Bussell Jr. (AnonJr)"
  avatar                 : "/assets/res/favicon.svg" # path of avatar image, e.g. "/assets/res/bio-photo.jpg"
  bio                    :
  location               : "Costal North Carolina, USA"
  email                  :
  # fediverse             : "@you@instance.social"  # used for fediverse:creator meta tag
  links:
    - label              : "About"
      icon               : "fa-regular fa-user"
      page_id            : about
    - label              : "CV"
      icon               : "fa-regular fa-file-lines"
      page_id            : cv
    - label              : "Instagram"
      icon               : "fa-brands fa-instagram"
      url                : "https://www.instagram.com/anonjr_live/"
    - label              : "Reddit"
      icon               : "fa-brands fa-reddit"
      url                : "https://www.reddit.com/user/AnonJr/"
    - label              : "Substack"
      icon               : "fa-solid fa-bookmark"
      url                : "https://substack.com/@anonjr"
    - label              : "Planet Minecraft"
      icon               : "fa-solid fa-earth-americas"
      url                : "https://www.planetminecraft.com/member/anonjr/"
    - label              : "X / Twitter"
      icon               : "fa-brands fa-square-x-twitter"
      url                : "https://x.com/AnonJr/"
    - label              : "Support"
      icon               : "fa-solid fa-gift"
      url                : "https://streamlabs.com/anonjr_live"

# Site Footer
footer:
  links:
    - label              : "GitHub"
      icon               : "fa-brands fa-github"
      url                : "https://github.com/AnonJr/"
    - label              : "Twitch"
      icon               : "fa-brands fa-twitch"
      url                : "https://www.twitch.tv/anonjr_live"
    - label              : "YouTube"
      icon               : "fa-brands fa-youtube"
      url                : "https://www.youtube.com/@anonjr"
    - label              : "Support"
      icon               : "fa-solid fa-gift"
      url                : "https://streamlabs.com/anonjr_live"
  since                  : "2001"


# Reading Files
include:
  - .htaccess
#  - _pages - Don't put this here unless you want the directory to be processed twice
exclude:
  # Environment-specific configuration
  - _config.dev.yml
  - _config.prod.yml

  # Build caches
  - .asset-cache
  - .bundle
  - .jekyll-assets-cache
  - .sass-cache

  # Development tooling
  - .editorconfig
  - .gitattributes
  - .github/
  - .gitignore
  - .ruby-version
  - .scripts/
  - .vscode/

  # Build dependencies
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/

  # Repository documentation
  - LICENSE.md
  - README.md

  # Deployment
  - wrangler.toml

encoding                 : "utf-8"
markdown_ext             : "markdown,mkdown,mkdn,mkd,md"

show_drafts              : false
future                   : false

# Conversion
markdown                 : kramdown
highlighter              : rouge
lsi                      : false
excerpt_separator        : <!--more-->
incremental              : false


# Markdown Processing - If GFM, https://kramdown.gettalong.org/quickref.html / https://github.com/kramdown/parser-gfm
kramdown:
  auto_ids               : true
  enable_coderay         : false
  entity_output          : as_char
  footnote_nr            : 1
  hard_wrap              : false
  input                  : GFM
  smart_quotes           : lsquo,rsquo,ldquo,rdquo
  toc_levels             : 1..6


# Sass/SCSS - https://github.com/jekyll/jekyll-sass-converter
sass:
  sass_dir               : _sass
  # style: compressed # Defined in _config.dev.yml and _config.prod.yml; https://sass-lang.com/documentation/file.SASS_REFERENCE.html#output_style
  quiet_deps             : true
  silence_deprecations   : ['import']


# Outputting
permalink                : /:year/:month/:title.html
date_format              : "%d %b %Y" # "%d %b %Y %H:%M" if I want to include time later
timezone                 : America/New_York # https://en.wikipedia.org/wiki/List_of_tz_database_time_zones


# Pagination with jekyll-paginate -  https://github.com/jekyll/jekyll-paginate
#paginate                 : 10 # amount of posts to show
#paginate_path            : /index/page:num/

# Pagination with jekyll-paginate-v2
# See https://github.com/sverrirs/jekyll-paginate-v2/blob/master/README-GENERATOR.md#site-configuration for configuration details
pagination:
  enabled                : false # Set enabled to true to use paginate v2
  debug                  : false
  collection             : 'posts'
  per_page               : 10
  permalink              : '/page/:num/'
  title                  : ':title - page :num'
  limit                  : 0
  sort_field             : 'date'
  sort_reverse           : true
  category               : 'posts'
  tag                    : ''
  locale                 : ''
  trail:
    before               : 2
    after                : 2


# Plugins (previously gems:)
plugins:
  - jekyll-seo-tag
  - jekyll-archives
  - jemoji
  - jekyll-redirect-from
  - jekyll-sitemap
  - jekyll-feed
  - jekyll-gist
  - jekyll-include-cache
  - jekyll-paginate


# Archives
#  Type
#  - GitHub Pages compatible archive pages built with Liquid ~> type: liquid (default)
#  - Jekyll Archives plugin archive pages ~> type: jekyll-archives
#  Path (examples)
#  - Archive page should exist at path when using Liquid method or you can
#    expect broken links (especially with breadcrumbs enabled)
#  - <base_path>/tags/my-awesome-tag/index.html ~> path: /tags/
#  - <base_path>/categories/my-awesome-category/index.html ~> path: /categories/
#  - <base_path>/my-awesome-category/index.html ~> path: /
category_archive:
  type                   : jekyll-archives
  path                   : /category/
tag_archive:
  type                   : jekyll-archives
  path                   : /tag/

# https://github.com/jekyll/jekyll-archives
tag_archive_path         : "/tag/:name"
jekyll-archives:
  enabled:
    - tags
  layouts:
    tag                  : tagpage
  permalinks:
    tag                  : /tag/:name/


# HTML Compression https://jch.penibelst.de/
# Put `compress_html: false` in the frontmatter to disable on an individual page/post.
compress_html:
  blanklines             : true
  clippings              : all
  comments               : all
  ignore:
    envs                 : [development]
  profile                : false

# Redirect old URLs after moving or renaming pages - https://github.com/jekyll/jekyll-redirect-from
redirect_from:
  json                   : false

# Collections
collections:
  pages:
    output               : true
    permalink            : /p/:title:output_ext
  styleguide:            # Defined in _config.dev.yml and _config.prod.yml, keeping a sensible default just in case
    output               : false

# Defaults
defaults:
  # _posts
  - scope:
      path               : "_posts"
      type               : posts
    values:
      layout             : single
      author_profile     : true
      read_time          : true
      show_date          : true
      comments           : # true
      share              : true
      show_taxonomy      : true
      related            : false
  # Pages defaults
  - scope:
      path               : "_pages"
      type               : pages
    values:
      layout             : project
      author_profile     : true
      share              : true
      permalink          : /p/:title:output_ext
